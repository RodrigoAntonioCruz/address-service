# Arquivos de texto genéricos (Java, properties, etc.)
*.java text diff=java
*.xml text
*.properties text
*.md text
*.yml text
*.yaml text
*.json text
*.txt text

# Shell scripts (Unix)
*.sh text eol=lf

# Batch/Windows scripts
*.bat text eol=crlf
*.cmd text eol=crlf

# Maven Wrapper
/mvnw text eol=lf
/mvnw.cmd text eol=crlf

# Excluir binários de conversão de EOL
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary

# Normaliza finais de linha automaticamente para arquivos de texto
* text=auto

# Estratégia de merge para arquivos de lock
*.lock merge=ours

# Expansão de palavras-chave (opcional, remova se não for necessário)
*.txt ident

# Diferenças personalizadas para JSON
*.json diff=json
